// Injetado no build pelo Vite (define em vite.config.ts) — SHA curto do commit
// na Vercel, ou 'dev' no ambiente local.
declare const __APP_VERSION__: string

// Fontes via @fontsource (só side-effect CSS; os pacotes não trazem tipos).
declare module '@fontsource-variable/geist'
declare module '@fontsource-variable/geist-mono'
